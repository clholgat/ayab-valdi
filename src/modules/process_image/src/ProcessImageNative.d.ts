/**
 * @ExportModule
 */
export declare function getBits(path: string): Uint8Array[][];
/** Read raw file bytes from a filesystem path (desktop native). */
export declare function readFileBytes(path: string): Uint8Array;
